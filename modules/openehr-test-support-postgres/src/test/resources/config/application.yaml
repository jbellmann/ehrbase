debug: false

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ehrbase //will be overridden by initializer
    username: ehrbase
    password: ehrbase
    hikari:
      driver-class-name: org.postgresql.Driver

  flyway:
    user: ehrbase
    password: ehrbase
    schemas:
      - ehr